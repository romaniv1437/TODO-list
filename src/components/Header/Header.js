import React from 'react';
import s from './header.module.css'

const Header = () => {
    return (
        <div className={s.header}>
            <h3>To Do List</h3>
        </div>
    );
};

export default Header;
